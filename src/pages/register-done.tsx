import React from 'react';

import { RegisterDone } from '@organisms/register-done';

import { Root } from '@modules/root';

export default function Home() {
  return (
    <Root>
      <RegisterDone />
    </Root>
  );
}
